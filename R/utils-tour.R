# .landing_page_tour ----

# data.frame that defines steps of the default tour
.landing_page_tour <- rbind(
    data.frame(
        element=paste0("#", "placeholder"),
        intro=paste0(
            "PLACEHOLDER")
        )
)